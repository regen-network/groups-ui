// import { type LinkProps as RouterLinkProps, Link as RouterLink } from 'react-router-dom'
// import Button, { type ButtonProps } from '@mui/material/Button'

// export const RouteButton = ({ to, ...props }: ButtonProps & RouterLinkProps) => {
//   return <Button component={RouterLink} to={to} {...(props as ButtonProps)} />
// }

export {}
